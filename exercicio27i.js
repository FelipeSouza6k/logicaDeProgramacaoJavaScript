alert("Ler dois valores inteiros e descbrir a diferença do primeiro pelo segundo");
let valor1 = parseInt(prompt("Digite o primeiro valor:"));
let valor2 = parseInt(prompt("Digite o segundo valor:"));

let diferenca = valor1 - valor2;
alert("A diferença entre " + valor1 + " e " + valor2 + " é: " + diferenca);